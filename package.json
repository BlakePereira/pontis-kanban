{
  "name": "pontis-kanban",
  "version": "1.0.0",
  "description": "Cloud-deployed kanban board for Pontis project management",
  "main": "server-cloud.js",
  "scripts": {
    "start": "node server-cloud.js",
    "dev": "nodemon server-cloud.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
